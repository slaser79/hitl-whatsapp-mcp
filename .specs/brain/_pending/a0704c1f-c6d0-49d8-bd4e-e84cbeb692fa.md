---
title: "CRITIC verification of SPEC-WAMCP-001 satellite (MISSION-2026-389) — SUCCESS, P-IMPL-4 deferred"
type: lesson
products: [hitl-whatsapp-mcp]
cause: ops-friction
source: a0704c1f-c6d0-49d8-bd4e-e84cbeb692fa
---
MISSION-2026-389 (WhatsApp Remote-MCP, SPEC-WAMCP-001) verified SUCCESS for delivered
server-side scope P-IMPL-1..3 at commit 699956f. All ACs verified live, not just by green
tests: http/sse start on loopback, auth gate 401→406, fail-closed guard exits non-zero on
all 4 unsafe configs, typed EC1/EC2/EC5 errors (ffmpeg→SystemDependencyError, not
chat_not_found), local-only DB, tailscale script fails fast + does not `serve reset` on
early exit. 78 tests pass, ruff clean, MIT chain intact.

Key disposition for the CoS/CEO: BO1/BO2 (phone read/send end-to-end) are **server-side
ready but not end-to-end verifiable here** — they depend on the explicitly §7-deferred
cross-repo phase **P-IMPL-4 in `slaser79/hitl-app`** (register remote MCP server URL+token).
Treat as "pending client integration", NOT a defect. AC5/AC6 (cross-tailnet reachability +
≤15-min timed dry-run) need a one-time live-tailnet/WhatsApp-QR mile on the CEO's host.

Ops-friction (no code bug): the satellite repo's CI is on a GitHub **fork**, so Actions
auto-run on PRs is blocked by GitHub's one-time fork UI-enable gate — each PR was verified
manually. `ci.yml` is present/correct. Owner should click "enable Actions" once on the fork.

decided_not_to_fix: AC1 CI fork-gate is a GitHub-platform artifact, not a code/spec defect; resolved by a one-time owner UI click rather than a code change.
